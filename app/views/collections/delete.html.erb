<% @page_title = "Delete Collection: #{@collection.collectionname}" %>

<%= link_to("<< Back to List", {:action => 'index'}, :class => 'back-link') %>

<div class="collection delete">
  <h2>Delete Collection</h2>
  
  <%= form_for(:collection, :url => {:action => 'destroy', :id => @collection.id}, :method => :delete) do |f| -%>

    <p>Are you sure you want to permanently delete this user?</p>
  
    <p class="reference-name"><%= @collection.name %></p>
  

      <%= submit_tag("Delete Collection", :class => "btn-primary") %>

    
  <% end %>
</div>